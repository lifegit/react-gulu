import React from 'react';
import { Icon } from 'antd-mobile';

export default () => (
  <div style={{textAlign:'center',padding:'15px 0 10px 0'}}>
      <Icon type={"loading"} size={'lg'} />
      <p>努力加载中...</p>
  </div>
);
